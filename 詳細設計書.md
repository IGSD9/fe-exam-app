# 基本情報技術者試験 過去問アプリ 詳細設計書

| 項目 | 内容 |
| --- | --- |
| 文書名 | 詳細設計書 |
| 対象システム | 基本情報技術者試験 過去問アプリ |
| 版数 | 1.0 |
| 作成日 | 2026-09-13 |
| 前提文書 | 要件定義書、基本設計書 |

---

## 1. 文書の目的

本書類は基本設計書を実装可能な粒度まで落とした内部設計である。開発者は第 14 章の実装手順に沿って進める。

---

## 2. ディレクトリ構成

```text
app/
  layout.tsx                  # 共通レイアウト、ボトムナビ
  page.tsx                    # S-01 初期化
  dashboard/page.tsx          # S-02
  practice/page.tsx           # S-03 年度・分野選択
  practice/[questionId]/page.tsx  # S-04 演習
  review/page.tsx             # S-05 弱点復習
  settings/page.tsx           # S-06 設定・課金
  api/
    questions/route.ts        # 問題一覧
    questions/[id]/route.ts   # 問題詳細
    answers/route.ts          # 解答保存
    dashboard/route.ts        # 集計
    review/route.ts           # 弱点抽出
    billing/webhook/route.ts  # RevenueCat Webhook
    billing/restore/route.ts  # 購入復元確認
components/
  ui/                         # ボタン、カード、グラフ
  quiz/QuestionCard.tsx
  quiz/OptionButton.tsx
  quiz/ExplanationPanel.tsx
  ads/BannerAd.tsx
  billing/PurchaseButton.tsx
  layout/BottomNav.tsx
  layout/LockOverlay.tsx
lib/
  supabase/client.ts
  supabase/server.ts
  auth.ts
  access.ts                   # 無料範囲判定
  billing.ts
  stats.ts
types/
  index.ts
supabase/
  migrations/                 # テーブル、RLS、インデックス
  seed.sql
```

初期資格 ID は `fe` とする。環境変数 `NEXT_PUBLIC_DEFAULT_QUALIFICATION_ID=fe` で切り替える。

---

## 3. 型定義

```ts
type QualificationId = string;

type QuestionOption = string;

type Question = {
  id: string;
  qualificationId: QualificationId;
  year: string;
  category: string;
  questionNumber: number;
  questionText: string;
  options: [QuestionOption, QuestionOption, QuestionOption, QuestionOption];
  correctIndex: 0 | 1 | 2 | 3;
  explanation: string | null;
  imageUrl: string | null;
};

type UserAnswer = {
  id: string;
  userId: string;
  questionId: string;
  selectedIndex: 0 | 1 | 2 | 3;
  isCorrect: boolean;
  answeredAt: string;
};

type UserSubscription = {
  userId: string;
  isPro: boolean;
  purchasedAt: string | null;
};

type DashboardStats = {
  totalAnswers: number;
  correctCount: number;
  accuracyPercent: number | null;
  recentWrongCount: number;
  categoryStats: Array<{
    category: string;
    answeredUnique: number;
    correctUnique: number;
    totalQuestions: number;
    achievementPercent: number;
  }>;
};
```

`options` は必ず 4 件。`correctIndex` は 0〜3。

---

## 4. データベース物理設計

### 4.1 確定 DDL

要件定義書の定義を実装用に補完する。

```sql
CREATE TABLE qualifications (
  id VARCHAR PRIMARY KEY,
  title VARCHAR NOT NULL,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);

CREATE TABLE questions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  qualification_id VARCHAR NOT NULL REFERENCES qualifications(id),
  year VARCHAR NOT NULL,
  category VARCHAR NOT NULL,
  question_number INT NOT NULL,
  question_text TEXT NOT NULL,
  options JSONB NOT NULL,
  correct_index INT NOT NULL,
  explanation TEXT,
  image_url TEXT,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT questions_options_is_array CHECK (jsonb_typeof(options) = 'array'),
  CONSTRAINT questions_correct_index_range CHECK (correct_index BETWEEN 0 AND 3),
  CONSTRAINT questions_unique_no UNIQUE (qualification_id, year, question_number)
);

CREATE TABLE user_answers (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
  question_id UUID NOT NULL REFERENCES questions(id),
  selected_index INT NOT NULL,
  is_correct BOOLEAN NOT NULL,
  answered_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  CONSTRAINT user_answers_selected_index_range CHECK (selected_index BETWEEN 0 AND 3)
);

CREATE TABLE user_subscriptions (
  user_id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  is_pro BOOLEAN NOT NULL DEFAULT FALSE,
  purchased_at TIMESTAMPTZ
);
```

### 4.2 インデックス

```sql
CREATE INDEX idx_questions_qual_year ON questions (qualification_id, year);
CREATE INDEX idx_questions_qual_category ON questions (qualification_id, category);
CREATE INDEX idx_user_answers_user_time ON user_answers (user_id, answered_at DESC);
CREATE INDEX idx_user_answers_user_question ON user_answers (user_id, question_id, answered_at DESC);
```

### 4.3 直近年度の持ち方

`year` は表示用文字列（例：`2025秋`）とは別に、比較用の並びが必要である。初期リリースでは次で扱う。

| 方式 | 内容 |
| --- | --- |
| 採用 | `year` は `YYYY` または `YYYY-H1` / `YYYY-H2`（例：`2025-H2`） |
| 直近年度 | 対象資格の `MAX(year)` |
| 表示 | `2025-H2` → 「2025年秋」のように画面で変換する |

無料開放対象は「`year = その資格の MAX(year)` の問題」とする。

### 4.4 RLS

```sql
ALTER TABLE qualifications ENABLE ROW LEVEL SECURITY;
ALTER TABLE questions ENABLE ROW LEVEL SECURITY;
ALTER TABLE user_answers ENABLE ROW LEVEL SECURITY;
ALTER TABLE user_subscriptions ENABLE ROW LEVEL SECURITY;

-- 資格マスタは認証済みなら参照可
CREATE POLICY qualifications_select ON qualifications
  FOR SELECT TO authenticated USING (true);

-- 問題の参照は認証済みに限定。本文の無料制限はサーバ側でも再チェックする
CREATE POLICY questions_select ON questions
  FOR SELECT TO authenticated USING (true);

-- 解答は本人のみ
CREATE POLICY user_answers_select ON user_answers
  FOR SELECT TO authenticated USING (auth.uid() = user_id);
CREATE POLICY user_answers_insert ON user_answers
  FOR INSERT TO authenticated WITH CHECK (auth.uid() = user_id);

-- 課金状態は本人のみ参照。更新はサービスロールのみ
CREATE POLICY user_subscriptions_select ON user_subscriptions
  FOR SELECT TO authenticated USING (auth.uid() = user_id);
```

`user_subscriptions` の INSERT / UPDATE はサービスロールキーを使うサーバ処理専用とする。クライアントからの直接更新は不可。

`questions` をクライアントから直接読む場合、無料ユーザーに過去問全文が渡る。そのため問題本文・解説・正解の取得は **サーバ Route Handler 経由を正** とし、クライアントの Supabase 直読みは一覧メタ情報（id, year, category, question_number）に限る。

---

## 5. 共通モジュール設計

### 5.1 `lib/access.ts` — 公開範囲判定

```ts
function getLatestYear(years: string[]): string | null
function canAccessYear(isPro: boolean, year: string, latestYear: string | null): boolean
async function getIsPro(userId: string): Promise<boolean>
```

**判定手順**

1. `user_subscriptions` から `is_pro` を取得する。行が無ければ `false`
2. 対象資格の `MAX(year)` を取得する
3. `isPro === true` なら常に許可
4. それ以外は `year === latestYear` のときだけ許可
5. `latestYear` が取れない（問題 0 件）ときは拒否

### 5.2 `lib/auth.ts` — 認証

```ts
async function ensureSession(): Promise<User>
async function linkAnonymousAccount(email: string, password: string): Promise<void>
async function signOutCurrentUser(): Promise<void>
```

**初回起動手順**

1. `getSession()` を呼ぶ
2. セッションがあればそのユーザーを返す
3. なければ `signInAnonymously()` を実行する
4. 失敗したら初期化画面に再試行を出す
5. 成功したら `/dashboard` へ遷移する

**本登録手順**

1. 設定画面でメールとパスワードを受け取る
2. 入力形式を検証する（メール形式、パスワード 8 文字以上）
3. 匿名ユーザーに対して認証手段をリンクする
4. 成功メッセージを出し、ユーザー種別を「本登録」に更新する
5. 失敗時は理由を表示し、匿名のまま継続する

### 5.3 `lib/billing.ts` — 課金

```ts
async function startPurchase(userId: string): Promise<void>
async function restorePurchases(userId: string): Promise<boolean>
async function applyProStatus(userId: string, purchasedAt: Date): Promise<void>
```

RevenueCat の Entitlement ID は `pro`、Product ID は `fe_unlock_all` とする。

---

## 6. API 詳細設計

共通：未ログインは `401`。JSON 形式は `{ data }` または `{ error: { code, message } }`。

### 6.1 GET `/api/dashboard`

**用途**：ダッシュボード指標取得。

**入力**：クエリ `qualificationId`（省略時は `fe`）。

**出力**：`DashboardStats`。

**処理手順**

1. セッションから `userId` を取得する
2. `qualificationId` を確定する
3. 対象資格の問題件数を分野別に集計する
4. 対象ユーザーの解答を対象資格の問題に絞る
5. 総解答数と正答数を数える
6. 正答率を算出する。0 件なら `accuracyPercent = null`
7. 分野ごとに「ユニーク正答問題数 / その分野の問題数」を算出する
8. 直近誤答件数（上限 20 件のユニーク問題数）を算出する
9. JSON で返す

**集計 SQL 方針**

```sql
-- 総解答・正答
SELECT
  COUNT(*) AS total_answers,
  COUNT(*) FILTER (WHERE ua.is_correct) AS correct_count
FROM user_answers ua
JOIN questions q ON q.id = ua.question_id
WHERE ua.user_id = $1
  AND q.qualification_id = $2;

-- 分野別
SELECT
  q.category,
  COUNT(DISTINCT q.id) AS total_questions,
  COUNT(DISTINCT ua.question_id) FILTER (WHERE ua.user_id = $1) AS answered_unique,
  COUNT(DISTINCT ua.question_id) FILTER (WHERE ua.user_id = $1 AND ua.is_correct) AS correct_unique
FROM questions q
LEFT JOIN user_answers ua ON ua.question_id = q.id
WHERE q.qualification_id = $2
GROUP BY q.category;
```

達成度 = `correct_unique / total_questions * 100`（小数第1位、分母 0 なら 0）。

### 6.2 GET `/api/questions`

**用途**：年度・分野選択用の一覧。本文は返さない。

**入力**：`qualificationId`, 任意で `year`, `category`。

**出力**：

```ts
{
  latestYear: string | null;
  isPro: boolean;
  items: Array<{
    id: string;
    year: string;
    category: string;
    questionNumber: number;
    locked: boolean;
  }>;
}
```

**処理手順**

1. 認証する
2. Pro 状態と直近年度を取得する
3. フィルタ条件で問題メタ情報を取得する
4. 各行に `locked = !canAccessYear(...)` を付ける
5. ロック中でも id は返してよいが、本文は絶対に含めない
6. `year`, `questionNumber` 昇順で返す

### 6.3 GET `/api/questions/:id`

**用途**：1 問の出題。

**無料ユーザーへの応答**

- 許可時：本文、選択肢、画像を返す。`correctIndex` と `explanation` は **未解答時は返さない**
- 拒否時：`403 ACCESS_DENIED`。本文なし

**Pro / 無料許可時の未解答レスポンス**

```ts
{
  id, year, category, questionNumber,
  questionText, options, imageUrl,
  isPro
}
```

正解と解説は解答 API の応答で返す。これにより解説チラ見を防ぐ。

**処理手順**

1. 認証する
2. 問題を ID で取得する。無ければ `404`
3. 資格がリクエスト対象と一致するか確認する
4. Pro と直近年度で閲覧可否を判定する
5. 不可なら 403
6. 可なら本文と選択肢だけ返す

### 6.4 POST `/api/answers`

**入力**

```ts
{ questionId: string; selectedIndex: 0 | 1 | 2 | 3 }
```

**出力**

```ts
{
  isCorrect: boolean;
  correctIndex: 0 | 1 | 2 | 3;
  explanation: string | null;
  selectedIndex: number;
}
```

**処理手順**

1. 認証する
2. `selectedIndex` が 0〜3 以外なら `400`
3. 問題を取得する。無ければ `404`
4. 公開範囲外なら `403`（解答も保存しない）
5. `isCorrect = (selectedIndex === question.correct_index)` をサーバで計算する
6. `user_answers` に INSERT する
7. 正誤、正解インデックス、解説を返す
8. INSERT 失敗時は `500`。画面には再試行を出す

同一問題の再解答も履歴として追加する（上書きしない）。ダッシュボードの総解答数は回数、達成度はユニーク正答で見る。

### 6.5 GET `/api/review`

**用途**：弱点復習の出題キュー作成。

**入力**：`qualificationId`, 任意 `limit`（既定 20、最大 50）。

**出力**

```ts
{ questionIds: string[]; count: number }
```

**抽出 SQL 方針**

```sql
SELECT DISTINCT ON (ua.question_id)
  ua.question_id
FROM user_answers ua
JOIN questions q ON q.id = ua.question_id
WHERE ua.user_id = $1
  AND q.qualification_id = $2
  AND ua.is_correct = FALSE
ORDER BY ua.question_id, ua.answered_at DESC;
```

その後、最新誤答日時の新しい順に並べ、`limit` 件に切る。

**処理手順**

1. 認証する
2. 上記で誤答問題 ID を取得する
3. 無料ユーザーは公開範囲外 ID を除外する
4. 残件を返す
5. 0 件なら空配列

クライアントは返却順に `GET /api/questions/:id` を呼び、通常演習と同じ UI で出題する。

### 6.6 POST `/api/billing/webhook`

**用途**：RevenueCat からの購入・復元イベントを受け、`is_pro` を更新する。

**処理手順**

1. 共有シークレットでリクエストを検証する
2. 対象イベントが `INITIAL_PURCHASE` / `NON_RENEWING_PURCHASE` / `PRODUCT_CHANGE` 等、Pro 付与に相当するか判定する
3. RevenueCat の `app_user_id` を自システムの `user_id` に解決する
4. Entitlement `pro` が active なら `is_pro = true`, `purchased_at = NOW()` で UPSERT する
5. 失効イベント（初期は買い切りのため基本発生しない）なら `is_pro = false` にする
6. `200` を返す。検証失敗は `401`

### 6.7 POST `/api/billing/restore`

**用途**：アプリ側リストア後の再同期。

**処理手順**

1. 認証する
2. RevenueCat REST API で当該ユーザーの Entitlement を確認する
3. `pro` が有効なら DB を Pro にする
4. 無効なら変更しない（誤って Pro をはがさない。問い合わせ対応は運用）
5. `{ isPro }` を返す

---

## 7. 画面詳細設計

### 7.1 共通レイアウト

| 要素 | 仕様 |
| --- | --- |
| ヘッダー | 画面名。演習中は「2 / 20」のような進捗 |
| ボトムナビ | ホーム / 演習 / 弱点 / 設定 |
| 広告枠 | 演習画面のみ。`isPro` で出し分け |
| ローディング | スケルトン |
| エラー | バナー + 再試行 |

### 7.2 S-01 初期化 `/`

**表示**：ロゴ、スピナー。

**処理手順**

1. ページマウント時に `ensureSession()` を呼ぶ
2. 成功したら `/dashboard` へ `replace` する
3. 失敗したら「再試行」ボタンを出す
4. 再試行で Step 1 に戻る

### 7.3 S-02 ダッシュボード `/dashboard`

**表示項目**

| 項目 | 状態 |
| --- | --- |
| 総解答数 | 数値 |
| 正答率 | `n%` または「未解答」 |
| 分野別グラフ | `categoryStats` |
| 復習カード | 「間違えた問題 N 件」 |
| CTA | 演習を始める / 弱点を復習する |

**処理手順**

1. `GET /api/dashboard` を呼ぶ
2. 読み込み中はスケルトンを出す
3. 成功したら数値とグラフを描画する
4. 失敗したらエラーと再試行を出す
5. 「演習を始める」で `/practice` へ
6. 「弱点を復習する」で `/review` へ

**空状態**：総解答数 0 のとき、グラフはプレースホルダ、「まずは 1 問解いてみましょう」を出す。

### 7.4 S-03 年度・分野選択 `/practice`

**表示**：年度タブまたはリスト、分野チップ、問題カード。ロックカードは薄く、錠アイコン付き。

**処理手順**

1. `GET /api/questions?qualificationId=fe` を呼ぶ
2. 年度ごとにグループ化する
3. 直近年度以外かつ `!isPro` のグループにロックを付ける
4. ロック行タップで `/settings` へ遷移する（クエリ `from=lock`）
5. 開放行タップで `/practice/{id}` へ遷移する

### 7.5 S-04 問題演習 `/practice/[questionId]`

**状態**

| 状態 | 意味 |
| --- | --- |
| `loading` | 問題取得中 |
| `ready` | 未解答。選択肢タップ可 |
| `answered` | 判定済。解説表示、次へ可 |
| `locked` | 403。購入導線 |
| `error` | 通信失敗 |

**UI 詳細**

- 選択肢は縦 4 ボタン
- 未選択：白背景
- 正解：緑 + 「正解」ラベル
- 不正解選択：赤 + 「不正解」ラベル
- 不正解時、正解選択肢も緑で示す
- 判定前は解説を出さない
- 判定後に `ExplanationPanel` を展開する
- 無料時のみ下部に `BannerAd`

**利用者手順（画面操作）**

1. 問題文を読む
2. A〜D のいずれかをタップする
3. 色とラベルで正誤を確認する
4. 解説を読む
5. 「次の問題」をタップする

**システム手順**

1. `GET /api/questions/:id` で出題データを取得する
2. 403 ならロック画面を出す
3. 選択肢タップでボタンを disabled にする
4. `POST /api/answers` に `questionId` と `selectedIndex` を送る
5. 応答の `isCorrect` / `correctIndex` / `explanation` で画面を `answered` にする
6. 同一年度・分野の次問 ID を一覧キャッシュまたは再取得で決める
7. 次問が無ければ選択画面へ戻る
8. 保存失敗時は選択肢を再度有効化し、エラーを出す

**次問の決め方**

1. 現在の `year`, `category` を保持する
2. 同じ条件の一覧を `questionNumber` 順に見る
3. 現在 ID の次を次問とする
4. 弱点モード（クエリ `mode=review`）ならレビューキューの次を使う

### 7.6 S-05 弱点復習 `/review`

**処理手順**

1. `GET /api/review` を呼ぶ
2. `count === 0` なら空状態。「演習へ」ボタンを出す
3. `count > 0` なら件数と「復習を始める」を出す
4. 開始で先頭 ID の `/practice/{id}?mode=review` へ遷移する
5. キューは `sessionStorage` に `reviewQuestionIds` として保存する
6. 最後の 1 問を解き終えたらダッシュボードへ戻し、「復習完了」トーストを出す

### 7.7 S-06 設定・課金 `/settings`

**表示**

| 項目 | 内容 |
| --- | --- |
| アカウント | 匿名 / 本登録 |
| プラン | 無料 / Pro |
| 購入 | 価格、特典説明、購入ボタン |
| 復元 | 購入復元 |
| 登録 | 匿名時のみ表示 |
| ログアウト | 本登録時のみ表示（匿名は出さない） |

**購入手順（アプリ）**

1. ユーザーが「全問題を解放する」をタップする
2. `PurchaseButton` が RevenueCat SDK の購入を開始する
3. ストアシートを表示する
4. 成功イベントを待つ
5. Webhook 反映を数秒ポーリングする（`GET /api/dashboard` または専用の me API で `isPro`）
6. `isPro === true` になったら完了メッセージを出す
7. 広告コンポーネントの表示フラグを落とす
8. 失敗・キャンセル時は無料のまま。メッセージを分ける（キャンセルは静かに閉じる）

**復元手順**

1. 「購入を復元」をタップする
2. RevenueCat の restore を実行する
3. `POST /api/billing/restore` で DB を同期する
4. 結果を画面に出す

---

## 8. コンポーネント設計

### 8.1 `QuestionCard`

| props | 型 | 役割 |
| --- | --- | --- |
| question | 出題用 Question（正解なし） | 本文と画像 |
| progress | `{ current, total }` | 進捗表示 |

画像があるときだけ `next/image` 相当で表示する。読み込み失敗時は画像なしで本文のみ。

### 8.2 `OptionButton`

| props | 役割 |
| --- | --- |
| index | 0〜3 |
| label | 選択肢テキスト |
| state | `idle` / `selected-correct` / `selected-wrong` / `reveal-correct` / `disabled` |
| onSelect | タップハンドラ |

判定後は全ボタン `disabled`。キーボード操作でも選択できるよう `button` 要素を使う。

### 8.3 `ExplanationPanel`

判定後のみマウントする。見出し「解説」、本文、正解ラベル（例：正解はウ）。

### 8.4 `BannerAd`

| props | 役割 |
| --- | --- |
| enabled | `!isPro` のとき true |

`enabled === false` なら `null`。広告 ID は環境変数 `NEXT_PUBLIC_ADMOB_BANNER_ID`。

### 8.5 `PurchaseButton`

タップで `startPurchase`。処理中はスピナーと disabled。

### 8.6 `LockOverlay`

「この年度は Pro で開放されます」と設定画面へのボタン。

---

## 9. 状態管理

| 状態 | 置き場 | 理由 |
| --- | --- | --- |
| セッション | Supabase クライアント | SDK 標準 |
| `isPro` | サーバ取得後、React Context | 広告とロックの共通参照 |
| 出題中の 1 問 | ページローカル state | 画面完結 |
| 復習キュー | `sessionStorage` | リロード耐性、タブを超えて残さない |
| 選択画面の一覧 | サーバ fetch キャッシュ | 次問決定に利用 |

グローバルなクライアント状態ライブラリは初期不要。`IsProProvider` のみ置く。

`isPro` の取得手順：

1. ログイン後に `user_subscriptions` を本人 RLS で 1 件読む、または dashboard API の `isPro` を使う
2. Context に格納する
3. 購入成功・復元成功で再取得する
4. 古い Context を信用して本文 API を省略しない。本文は常にサーバ判定

---

## 10. エラーコード

| code | HTTP | 意味 | 画面 |
| --- | --- | --- | --- |
| `UNAUTHENTICATED` | 401 | 未ログイン | 初期化へ戻す |
| `ACCESS_DENIED` | 403 | 無料範囲外 | 課金導線 |
| `NOT_FOUND` | 404 | 問題なし | 一覧へ戻す |
| `VALIDATION_ERROR` | 400 | 入力不正 | 入力箇所に表示 |
| `PURCHASE_UNVERIFIED` | 400 | 購入未検証 | 復元案内 |
| `INTERNAL_ERROR` | 500 | サーバエラー | 再試行 |

---

## 11. 広告・ネイティブ変換時の注意

| 実行形態 | 広告 | 課金 |
| --- | --- | --- |
| Web（開発） | プレースホルダバナー | RevenueCat サンドボックス、または Pro フラグの手動更新 |
| PWA | Web 向け広告は初期はプレースホルダ可 | Web 課金は初期対象外でもよい。要件の本命はストア課金 |
| Capacitor / RN 変換後 | AdMob ネイティブバナー | RevenueCat SDK + ストア商品 |

基本設計のマネタイズを満たす本実装は **ネイティブ変換後の IAP + AdMob** である。Web 単体フェーズでは同じ画面契約（`isPro` でロックと広告を切る）だけ先に実装する。

---

## 12. 他資格へ展開するとき

1. `qualifications` に新しい ID を追加する（例：`diver`）
2. その ID で問題を投入する
3. 環境変数のデフォルト資格を切り替える、または資格選択画面を足す
4. ストア商品と AdMob ユニットをアプリ単位で用意する
5. 画面コピー（試験名）を資格マスタ `title` から出す

問題 UI・解答・ダッシュボード・弱点・課金フラグはそのまま再利用する。

---

## 13. テスト観点

| 項番 | ケース | 期待 |
| --- | --- | --- |
| T-01 | 初回起動 | 匿名ログインされダッシュボードに着く |
| T-02 | 無料で直近年度 | 本文が表示される |
| T-03 | 無料で過去年度 | 403、本文なし、課金導線 |
| T-04 | 4 択解答 | 即時色分け、解説表示、履歴 1 件追加 |
| T-05 | 正解改ざん | クライアントが `is_correct=true` を付けてもサーバ計算が優先 |
| T-06 | ダッシュボード | 総数・正答率・分野達成度が履歴と一致 |
| T-07 | 弱点復習 | 直近誤答が新しい順に出る。重複なし |
| T-08 | 弱点 0 件 | 空状態 |
| T-09 | 購入成功 | `is_pro=true`、全年度開放、広告なし |
| T-10 | 購入キャンセル | 状態変化なし |
| T-11 | 本登録 | 解答履歴が同じ user_id のまま残る |
| T-12 | 広告失敗 | 演習は継続できる |

---

## 14. 実装手順（開発 Step）

ここから先は、実装担当が上から順に進める作業手順である。

### Step 1. リポジトリとアプリ骨格

1. `npx create-next-app` で App Router / TypeScript / Tailwind を有効にして作成する
2. `app/layout.tsx` にフォントと共通余白を入れる
3. ボトムナビの 4 リンクだけ先に置く
4. 各ページに見出しだけ出してルーティングを確認する
5. `.env.local` に Supabase / RevenueCat / AdMob / デフォルト資格 ID のキー名を書く

### Step 2. Supabase スキーマ

1. Supabase プロジェクトを作る
2. Authentication で Anonymous sign-in を ON にする
3. 第 4 章の DDL を SQL Editor または migration で実行する
4. RLS ポリシーを適用する
5. `qualifications` に `('fe', '基本情報技術者試験')` を入れる
6. 直近年度 2 問、過去年度 2 問のサンプルを `questions` に入れる
7. Table Editor で 4 テーブルが見えることを確認する

### Step 3. 認証を通す

1. `lib/supabase/client.ts` と `server.ts` を用意する
2. `ensureSession()` を実装する
3. `/` で匿名ログインし、`/dashboard` へ飛ばす
4. ブラウザで起動し、Supabase Authentication に匿名ユーザーが 1 件増えることを確認する
5. 設定画面に「匿名ユーザー」表示を出す
6. メール登録（リンク）を接続する
7. 登録後も同じ `user_id` であることを Authentication 画面で確認する

### Step 4. 公開範囲判定を先に単体で書く

1. `getLatestYear` と `canAccessYear` を実装する
2. Pro なら全年度 true、無料なら最新年度だけ true になるテストを書く
3. この関数を API から必ず呼ぶ

### Step 5. 問題一覧 API と選択画面

1. `GET /api/questions` を実装する（本文なし）
2. `/practice` で年度・分野・ロックを表示する
3. 匿名の無料ユーザーで、過去年度がロックされることを確認する
4. ロックタップで設定画面に行くことを確認する

### Step 6. 出題 API と演習画面

1. `GET /api/questions/:id` を実装する。未解答時は正解・解説を含めない
2. `QuestionCard` と `OptionButton` を実装する
3. 無料ユーザーで直近年度の本文が出ること、過去年度が 403 になることを確認する
4. ブラウザの Network タブで、403 応答に `question_text` が無いことを確認する

### Step 7. 解答保存

1. `POST /api/answers` を実装する。正誤はサーバ計算
2. 画面を `ready` → `answered` に遷移させる
3. 解説パネルを判定後に出す
4. Supabase の `user_answers` に行が増えることを確認する
5. わざとクライアントから不正な `selectedIndex`（4 など）を送り、400 になることを確認する

### Step 8. ダッシュボード

1. `GET /api/dashboard` の集計 SQL を実装する
2. 総解答数、正答率、分野別バーを描画する
3. 何問か解いたあと、数値が一致するか検算する
4. 未解答ユーザーで「未解答」表示になることを確認する

### Step 9. 弱点復習

1. `GET /api/review` を実装する
2. 意図的に 2 問間違え、1 問は 2 回間違える
3. キューがユニーク 2 件になり、新しい誤答が先になることを確認する
4. `/review` → 演習（`mode=review`）→ 次問 → 完了の順を通す
5. 誤答 0 のユーザーで空状態を確認する

### Step 10. 課金フラグと広告出し分け

1. `user_subscriptions` をサービスロールで UPSERT する関数を書く
2. 開発中は管理用に、自分の `user_id` を SQL で `is_pro = true` にする
3. Pro になると過去年度の `locked` が外れ、広告が消えることを確認する
4. 再度 `false` に戻し、制限が復活することを確認する

### Step 11. RevenueCat 接続

1. RevenueCat にアプリと Entitlement `pro`、商品 `fe_unlock_all` を登録する
2. クライアントに SDK を入れ、ユーザー ID を Supabase `user.id` と一致させる
3. サンドボックス購入を 1 回行う
4. Webhook で `user_subscriptions` が更新されることを確認する
5. 復元ボタンから `restore` → サーバ同期まで通す
6. 購入キャンセルで DB が変わらないことを確認する

### Step 12. AdMob

1. テスト用バナー ID を設定する
2. `BannerAd` を演習画面だけに置く
3. 無料で表示、Pro で非表示を確認する
4. 広告 SDK 失敗を模擬し、演習が止まらないことを確認する

### Step 13. 仕上げと受け入れ

1. 要件定義書の全画面を一通り操作する
2. 第 13 章の T-01〜T-12 をチェックする
3. モバイル幅（375px）と PC 幅でレイアウトを確認する
4. 問題画像あり / なしの両方を確認する
5. 他資格用に `qualifications` をもう 1 件足し、ID 切替だけで一覧が変わることを確認する

---

## 15. 運用投入手順（問題データ）

本番の過去問を入れるときの手順。

1. 公式の出題年度・分野・問題番号を確認する
2. 本文、4 択、正解インデックス（0〜3）、解説、画像を CSV または JSON に整える
3. `qualification_id = 'fe'` を付ける
4. `year` を `YYYY-H1` / `YYYY-H2` に正規化する
5. `options` が配列 4 件、`correct_index` が範囲内かバリデーションする
6. staging の `questions` に投入する
7. 直近年度の `MAX(year)` が意図した年度になるか確認する
8. 無料アカウントで開放範囲を目視する
9. 本番へ投入する

画像がある場合：

1. Supabase Storage の公開バケット `question-images` に上げる
2. 公開 URL を `image_url` に書く
3. 演習画面で表示と代替テキスト（「問題図」）を確認する

---

## 16. 詳細設計の確定事項まとめ

| 項目 | 確定内容 |
| --- | --- |
| 無料開放 | 対象資格の `MAX(year)` のみ |
| 正解の権威 | `questions.correct_index`。サーバ再計算 |
| 解説表示 | 解答 API 成功後のみ |
| 課金の権威 | RevenueCat 検証後の `user_subscriptions.is_pro` |
| 広告 | `is_pro = false` の演習画面下部 |
| 弱点 | 直近誤答を問題単位でユニーク化。上限 20 |
| 履歴 | 再解答は追記。達成度はユニーク正答 |
| 横展開 | `qualifications.id` の追加と問題投入 |
